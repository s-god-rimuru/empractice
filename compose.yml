version: '3'

services:
    web:
      image: nginx:latest
      ports:
        - "8080:80"
    db:
      image: mysql
      ports:
        - "3306:3306"
      environment:
          - MYSQL_ROOT_PASSWORD=root123
          - MYSQL_DATABASE='school'
